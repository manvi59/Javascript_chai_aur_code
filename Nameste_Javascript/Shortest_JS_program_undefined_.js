//  shortest js program in javscript is  nothing  which assiging global object  window ( this )

// undefined --- it happend when in memeory box ------ variable's stored in memory with assigning undefined value

console.log(k); //  undefined

var k = 10;
