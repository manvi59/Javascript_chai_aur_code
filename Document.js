//  DOM--document object Model  -- it is interface of an HTML . which  represent the structure of the HTML / XML in tree format

// The Document Object Model (DOM) is a programming interface that allows programs to change the structure, style, and content of a web page. It's a key part of making websites interactive.

// by the helop of DOM we can modify web page uisng tree like strinure htML

// ------------worked in inspact
console.log(window); //global Object
console.log(window.document); //provide the HTML content in tree format
